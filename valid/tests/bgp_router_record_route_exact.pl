use strict;

return ["bgp router record-route (exact-match)",
	"cbgp_valid_bgp_router_record_route_exact"];

# -----[ cbgp_valid_bgp_router_record_route_exact ]------------------
#
# -------------------------------------------------------------------
sub cbgp_valid_bgp_router_record_route_exact($) {
  my ($cbgp)= @_;

  return TEST_TODO;
}

